{
    "config": {
        "preferred-install": "dist"
    },
    "repositories": [
        {
            "type": "composer",
            "url": "https://wpackagist.org"
        }
    ],
    "require": {
        "php": ">=5.6",
        "composer/installers": "~1.0.12",
        "vlucas/phpdotenv": "^2.0.1",
        "johnpbloch/wordpress": "4.7",
        "oscarotero/env": "^1.0",
        "roots/wp-password-bcrypt": "1.0.0",
        "wp-cli/wp-cli": "^0.25",
        "psy/psysh": " ^0.7.2"
    },
    "require-dev": {
        "squizlabs/php_codesniffer": "^2.5.1"
    },
    "extra": {
        "installer-paths": {
            "public_html/app/mu-plugins/{$name}/": ["type:wordpress-muplugin"],
            "public_html/app/plugins/{$name}/": ["type:wordpress-plugin"],
            "public_html/app/themes/{$name}/": ["type:wordpress-theme"]
        },
        "wordpress-install-dir": "public_html/wp"
    },
    "scripts": {
        "test": [ "vendor/bin/phpcs --ignore=public_html/wp/,vendor/ -n -s ."]
    }
}
